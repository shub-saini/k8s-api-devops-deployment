global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'api'
    static_configs:
      - targets: ['api:3000']
        labels:
          service: 'api'

    # Scrape metrics
    metrics_path: '/metrics'
    scrape_interval: 10s
