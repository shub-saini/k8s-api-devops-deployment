apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: gcp-secret-store
spec:
  provider:
    gcpsm:
      projectID: staging-24813
      auth:
        workloadIdentity:
          serviceAccountRef:
            name: external-secrets-operator-sa
            namespace: external-secrets-system
